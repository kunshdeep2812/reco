# reco CLI output
/Output/*
!/Output/Don't delete this folder

# dashboard runtime state (SQLite DB, encryption key, admin password, logs) - never commit these
/dashboard/data/*.sqlite3
/dashboard/data/*.sqlite3-*
/dashboard/data/secret.key
/dashboard/data/admin_credentials.txt
/dashboard/logs/*.log

*.gem
.bundle/
